{
  "name": "@ryb73/bs-bluebird",
  "version": "0.0.2",
  "description": "BuckleScript bindings for bluebird",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run clean && bsb -make-world",
    "watch": "npm run clean && bsb -make-world -w",
    "clean": "rm -rf lib && bsb -clean-world",
    "postinstall": "[ ! -f package-links.json ] || ppl link-file"
  },
  "files": [
    "bsconfig.json",
    "src",
    "/lib/js/src"
  ],
  "keywords": [
    "bucklescript",
    "reason",
    "reasonml",
    "promise"
  ],
  "license": "MIT",
  "author": "Ryan Biwer",
  "_": [],
  "repository": {
    "type": "git",
    "url": "git@github.com:ryb73/bs-bluebird.git"
  },
  "peerDependencies": {
    "bs-platform": "^4.0.0"
  },
  "dependencies": {
    "bluebird": "^3.5.1"
  }
}
